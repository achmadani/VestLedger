{
  "name": "vestledger",
  "version": "0.1.0",
  "private": true,
  "description": "Asset pipeline untuk VestLedger. Node.js hanya dibutuhkan saat development/build, TIDAK dibutuhkan di production shared hosting (lihat DEPLOYMENT.md).",
  "scripts": {
    "dev": "tailwindcss -i ./resources/css/app.css -o ./public/assets/css/app.css --watch",
    "build:css": "tailwindcss -i ./resources/css/app.css -o ./public/assets/css/app.css --minify",
    "build:js": "node ./resources/copy-vendor.mjs",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "alpinejs": "^3.14.0",
    "daisyui": "^5.0.0",
    "tailwindcss": "^4.1.0"
  }
}
